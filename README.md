# 赛博墓碑 (CyberGrave)

一个简单易用的静态网页生成工具，用于创建纪念页面，缅怀某个人或某件事。

## ✨ 特性

- 🎨 精美的深色主题设计
- 📝 简单的 JSON 配置文件
- 🖼️ 照片画廊展示
- 📅 人生时间线
- 🕯️ 互动烛光功能
- 💬 访客留言（本地存储）
- 🎵 背景音乐播放
- 📱 响应式设计，支持移动端
- 🚀 纯静态文件，无需服务器

## 📦 安装

### 前置要求

- Node.js (版本 12 或更高)

### 克隆项目

```bash
git clone https://github.com/your-username/CyberGrave.git
cd CyberGrave
```

### 安装依赖

```bash
npm install
```

## 🚀 快速开始

### 1. 配置纪念信息

复制示例配置文件：

```bash
cp config.example.json config.json
```

编辑 `config.json` 文件，填写纪念对象的信息：

```json
{
  "basic": {
    "name": "纪念对象名称",
    "dates": "1990-2023",
    "subtitle": "永远怀念",
    "description": "这里写下纪念对象的简介或故事..."
  },
  "theme": {
    "name": "dark",
    "accentColor": "#4a9eff"
  },
  "music": {
    "enabled": true,
    "file": "assets/music.mp3",
    "autoplay": false
  },
  "photos": [
    "assets/photo1.jpg",
    "assets/photo2.jpg",
    "assets/photo3.jpg"
  ],
  "timeline": [
    {
      "year": "1990",
      "event": "出生"
    },
    {
      "year": "2008",
      "event": "大学毕业"
    },
    {
      "year": "2023",
      "event": "永远离开"
    }
  ],
  "messages": {
    "enabled": true,
    "title": "访客留言"
  },
  "candle": {
    "enabled": true,
    "title": "点亮烛光"
  }
}
```

### 2. 添加资源文件

将照片和音乐文件放入 `src/assets/` 目录：

- 照片：`photo1.jpg`, `photo2.jpg`, `photo3.jpg` 等
- 音乐：`music.mp3`

### 3. 生成网页

运行生成命令：

```bash
npm run generate
```

或直接使用 Node.js：

```bash
node tools/generate.js
```

### 4. 查看网页

生成的网页文件位于 `output/` 目录，在浏览器中打开：

```bash
open output/index.html
```

## 📁 项目结构

```
CyberGrave/
├── src/
│   ├── templates/        # 网页模板
│   │   ├── template.html # HTML 模板
│   │   └── script.js     # JavaScript 脚本
│   ├── themes/          # 主题样式
│   │   └── dark.css     # 深色主题
│   └── assets/          # 静态资源（照片、音乐）
├── tools/               # 工具脚本
│   └── generate.js      # 网页生成工具
├── output/              # 生成的网页文件（自动生成）
├── config.json          # 配置文件（需自行创建）
├── config.example.json  # 配置文件示例
├── package.json         # 项目配置
└── README.md           # 使用说明
```

## ⚙️ 配置说明

### 基本信息配置 (basic)

| 字段 | 说明 | 示例 |
|------|------|------|
| name | 纪念对象名称 | "张三" |
| dates | 生卒年份 | "1990-2023" |
| subtitle | 副标题 | "永远怀念" |
| description | 详细描述 | "纪念对象的简介或故事" |

### 主题配置 (theme)

| 字段 | 说明 | 示例 |
|------|------|------|
| name | 主题名称 | "dark" |
| accentColor | 强调色 | "#4a9eff" |

### 音乐配置 (music)

| 字段 | 说明 | 示例 |
|------|------|------|
| enabled | 是否启用 | true/false |
| file | 音乐文件路径 | "assets/music.mp3" |
| autoplay | 是否自动播放 | true/false |

### 照片配置 (photos)

照片文件路径数组，支持多个照片。

### 时间线配置 (timeline)

时间线事件数组，每个事件包含：
- `year`: 年份
- `event`: 事件描述

### 留言配置 (messages)

| 字段 | 说明 | 示例 |
|------|------|------|
| enabled | 是否启用 | true/false |
| title | 标题 | "访客留言" |

### 烛光配置 (candle)

| 字段 | 说明 | 示例 |
|------|------|------|
| enabled | 是否启用 | true/false |
| title | 标题 | "点亮烛光" |

## 🌐 部署

### 本地部署

直接在浏览器中打开 `output/index.html` 文件即可。

### GitHub Pages

1. 将 `output/` 目录的内容推送到 GitHub 仓库
2. 在仓库设置中启用 GitHub Pages
3. 选择 `output/` 目录作为发布源

### Netlify

1. 将 `output/` 目录拖拽到 Netlify 部署页面
2. 或将项目连接到 Netlify 并设置发布目录为 `output/`

## 🎨 自定义主题

编辑 `src/themes/dark.css` 文件来自定义样式。

主要颜色变量：

```css
:root {
    --bg-color: #0a0a0f;
    --text-color: #e0e0e0;
    --accent-color: #4a9eff;
    --secondary-color: #2a2a3a;
    --border-color: #3a3a4a;
}
```

## 🔧 常见问题

### Q: 留言功能的数据存储在哪里？

A: 留言数据存储在浏览器的 localStorage 中，仅保存在访问者的本地浏览器中。如果需要持久化存储，需要添加后端服务。

### Q: 如何修改主题颜色？

A: 编辑 `config.json` 中的 `theme.accentColor` 字段，或直接修改 `src/themes/dark.css` 文件。

### Q: 支持哪些图片格式？

A: 支持常见的图片格式，如 JPG、PNG、GIF、WebP 等。

### Q: 如何禁用音乐自动播放？

A: 在 `config.json` 中设置 `music.autoplay` 为 `false`。

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 💡 使用场景

- 纪念逝去的亲人或朋友
- 纪念宠物
- 纪念重要事件
- 创建个人纪念主页
- 团队纪念页面

## 📮 联系方式

如有问题或建议，欢迎通过以下方式联系：

- 提交 Issue
- 发送邮件

---

**赛博墓碑 - 永恒的纪念**
